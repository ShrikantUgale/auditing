extends ../layout

block content
  h1 #{title}
  ul.list-group
    each pfc, i in pfcs
      li.list-group-item
        a(href="/pfc/"+pfc._doc._id)= pfc.description
        //- a(href="/articles/"+pfc._id)= pfc.machineNum
